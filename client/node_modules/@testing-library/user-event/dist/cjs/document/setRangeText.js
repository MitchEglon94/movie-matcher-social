'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var interceptor = require('./interceptor.js');
var selection = require('./selection.js');
var value = require('./value.js');

function prepareRangeTextInterceptor(element) {
    interceptor.prepareInterceptor(element, 'setRangeText', function interceptorImpl(...realArgs) {
        return {
            realArgs,
            then: ()=>{
                value.setUIValueClean(element);
                selection.setUISelectionClean(element);
            }
        };
    });
}

exports.prepareRangeTextInterceptor = prepareRangeTextInterceptor;
